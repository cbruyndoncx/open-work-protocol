# Copy to `env.local` (recommended for Obsidian) or `.env` and fill in secrets.

# Admin token for scheduler write operations (used by `pool admin ...`).
OWP_ADMIN_TOKEN=dev-admin

# Optional: GitHub API token for importing issues (used by `pool admin import-github-issues ...`).
GITHUB_TOKEN=ghp_your_token_here

